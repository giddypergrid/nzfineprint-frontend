# Base URL of the Fine Print API. Leave empty in dev — the Vite proxy (vite.config.ts)
# forwards /search, /ask and /stats to the backend. Set this only when the built site is served
# from a different origin than the API (production), e.g. VITE_API_BASE=https://api.example.com
VITE_API_BASE=
